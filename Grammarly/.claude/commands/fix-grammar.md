---
description: Fix grammar, spelling, and punctuation in the given text while preserving tone and meaning.
argument-hint: "[text to fix, or leave blank to use the text already in context]"
---

Fix the grammar, spelling, and punctuation in the following text. Correct only
genuine errors — do not rewrite the tone, register, or sentence structure
beyond what's needed to fix them. Preserve formatting (line breaks, lists,
markdown).

Text:
$ARGUMENTS

If no text was provided above, use the most recently shared text or file in
this conversation. Output the corrected text, followed by a short list of
what changed and why (skip the list if there's nothing to note).
